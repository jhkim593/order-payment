spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:30036/user
    username: root
    password: a1234567!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_batch_fetch_size: 100

  flyway:
    enabled: true
    baseline-on-migrate: true

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: user-service-group

server:
  port: 8083